{
  "name": "local-brand-builder-backend",
  "version": "1.0.0",
  "description": "Backend for Local Brand Builder",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "db:setup": "npx prisma init --datasource-provider sqlite && npx prisma migrate dev --name init"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@prisma/client": "^5.7.1",
    "prisma": "^5.7.1",
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
